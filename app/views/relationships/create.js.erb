follow_form = document.querySelector('#follow_form')
follow_form.innerHTML = "<%= escape_javascript(render('users/unfollow'))%>"

followers = document.querySelector('#followers')
followers.innerHTML = "<%= @user.followers.count %>"
