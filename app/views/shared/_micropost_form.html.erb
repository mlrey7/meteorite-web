<%= form_for(@micropost) do |f| %>
  <div class="form-group">
    <%= f.text_area :content, placeholder: "What's poppin'?", class: "form-control" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary action-btn" %>
<% end %>
